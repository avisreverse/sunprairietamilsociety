/**
 * Root page — redirects to /en by default via next-intl middleware.
 * @see D-007 — next-intl routing middleware handles locale redirect
 */
export default function RootPage() {
  return null;
}
